<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:padding="16dp"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <Button
        android:id="@+id/addBtn"
        android:text="Add Movie Review"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"/>

    <ListView
        android:id="@+id/listView"
        android:layout_width="match_parent"
        android:layout_height="200dp"/>

    <!-- TABLE TO SHOW DETAILS -->
    <TableLayout
        android:id="@+id/table"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:stretchColumns="1"
        android:layout_marginTop="20dp">

        <TableRow>
            <TextView android:text="Movie Name"/>
            <TextView android:id="@+id/tName"/>
        </TableRow>

        <TableRow>
            <TextView android:text="Year"/>
            <TextView android:id="@+id/tYear"/>
        </TableRow>

        <TableRow>
            <TextView android:text="Rating"/>
            <TextView android:id="@+id/tRating"/>
        </TableRow>

    </TableLayout>

</LinearLayout>